services:
  foods:
    build:
      context: ./
      dockerfile: Dockerfile
    restart: always
    environment:
      - POSTGRES_CONNECTION_STRING=postgresql://postgres:password@postgres:5432/food_db